import React from 'react';
import Filters from './Filters';

const Home = () => (
  <div>
    <header>
      <h3>StarWars Datatable with Filters</h3>
    </header>
    <Filters />
  </div>
);

export default Home;
